from django.apps import AppConfig


class CourseAssignmentsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.course_assignments"
    label = "course_assignments"
